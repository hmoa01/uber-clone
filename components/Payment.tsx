import { Text, View } from 'react-native';

import CustomButton from './CustomButton';

const Payment = () => {
  return (
    <>
      <CustomButton title="Confirm Ride" className="my-10" onPress={() => {}} />
    </>
  );
};

export default Payment;
